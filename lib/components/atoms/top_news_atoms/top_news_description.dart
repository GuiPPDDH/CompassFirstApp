import 'package:flutter/material.dart';

class TopNewsDescription extends StatelessWidget {
  const TopNewsDescription({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
